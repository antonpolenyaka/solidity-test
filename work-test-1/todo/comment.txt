Вот это может помочь, комментарии к заданию

1) не описано, что такое и зачем переменная connectors, предполагаю, что промежуточные токены, которые можно использовать на пути tokenIn -> tokenOut
2) не описано Uniswap-like какой версии? Предполагаю, что автор имел в виду Uniswap-v2 как самый популярный и имеющий много клонов, но это стОило уточнить напрямую, чтоб народ не пытался смотреть в актуальный сейчас Uniswap-v3 и не пытался бы искать его клоны.
3) pragma solidity >=0.8.9; - это будет сильно мешать разработке, так как юнисвап-v2 написан на несовместимой 0.6.7, и это затруднит использование его библиотек

Опционально:
4) amountOutMin желательно сделать массивом размером path.length-1, так как на каждом свапе будет свое значение amountOutMin. Но возможно здесь типа для простоты взяли последнее значение. 
5) address[] public routers;
    address[] public connectors; нужны только в первой функции, так что их можно бы было сделать параметрами этой функции, а не полями контракта.
